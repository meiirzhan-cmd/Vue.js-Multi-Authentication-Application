# Backend Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# Server Configuration
# ===========================================
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:5174

# ===========================================
# Database (PostgreSQL)
# ===========================================
# Local: postgresql://USER:PASSWORD@localhost:5432/DATABASE?schema=public
# Neon: postgresql://USER:PASSWORD@xxx.neon.tech/DATABASE?sslmode=require
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/multiauth?schema=public

# ===========================================
# Redis (for sessions)
# ===========================================
# Local: redis://localhost:6379
# Upstash: rediss://default:PASSWORD@xxx.upstash.io:6379
REDIS_URL=redis://localhost:6379

# ===========================================
# Session & JWT Secrets
# ===========================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-session-secret-min-32-characters
JWT_SECRET=your-jwt-secret-min-32-characters
JWT_EXPIRES_IN=7d

# ===========================================
# Google OAuth
# ===========================================
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# ===========================================
# Magic Link (Passwordless Auth)
# ===========================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MAGIC_LINK_SECRET=your-magic-link-secret-min-32-characters
MAGIC_LINK_EXPIRES_IN=15m

# ===========================================
# Email (SMTP for Magic Links)
# ===========================================
# Gmail: smtp.gmail.com (use App Password, not regular password)
# Outlook: smtp.office365.com
# SendGrid: smtp.sendgrid.net
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=your-email@gmail.com
